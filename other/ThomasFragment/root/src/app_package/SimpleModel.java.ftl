package ${packageName}.fragment.model;

import ${packageName}.fragment.contract.${pageName}Contract;

/**
 * @describe
 * @author Thomas
 * @date ${.now?string["yyyy/MM/dd"]}
 * @updatelog
 * @since
 */
public class ${pageName}Model implements ${pageName}Contract.Model {
}
